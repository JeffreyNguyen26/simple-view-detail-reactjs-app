version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:12.20.2
    working_directory: ~/repo

    steps:
      - checkout
      - run: 
          name: clean cache
          command: npm cache clean --force
      # - run:
      #     name: Install Dependencies
      #     command: npm install
      # - run:
      #     name: install
      #     command: npm ci
      - run:
          name: npm install
          command: npm install
      - run:
          name: build
          command: npm run-script build

      # - restore_cache:
      #     keys:
      #       - v1-dependencies-{{ checksum "package.json" }}
      #       # fallback to using the latest cache if no exact match is found
      #       - v1-dependencies-

      # - save_cache:
      #     paths:
      #       - node_modules
      #       - ~/.npm
      #       - ~/.cache
      #     key: v1-dependencies-{{ checksum "package.json" }}
      - store_artifacts:
          path: build/static/
          destination: BuildResult

  deploy:
    docker:
      - image: circleci/node:12.20.2
    working_directory: ~/repo
    steps:
      - checkout
      # - run: |-
      #     cat >> ~/.netrc \<<EOF
      #       machine api.heroku.com
      #         login thangnldse62897@fpt.edu.vn
      #         password Th@ng26121998
      #       machine git.heroku.com
      #         login thangnldse62897@fpt.edu.vn
      #         password Th@ng26121998
      #     EOF
      # - run: chmod 600 ~/.netrc 

      # - run:
      #     name: Clone from Heroku
      #     command: git clone https://heroku:a8f4f414-8b90-4f3a-aa4d-6dc7e35fceae@git.heroku.com/simple-view-detail-reactjs-app.git main
            
      - run:
          name: Deploy Master to Heroku
          command: |
            git fetch --all --prune
            git pull origin main
            git push https://heroku:a8f4f414-8b90-4f3a-aa4d-6dc7e35fceae@git.heroku.com/simple-view-detail-reactjs-app.git main

workflows:
  version: 2.1
  workflow:
    jobs:
      # - build
      - deploy

